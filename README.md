# Hagen

Запись и расшифровка совещаний локально на компьютере. Программа пишет микрофон и
звук звонка, распознаёт речь, обрабатывает записи или ссылки, разводит реплики по говорящим, делает из
стенограммы документы (протокол, саммари, конспект) и кладёт их заметкой в
Obsidian.

**Всё распознаётся локально.** Звук совещаний никуда не уходит: модели
распознавания работают на этом компьютере, без интернета. В сеть уходит только
текст стенограммы и только тогда, когда вы собираете из неё документ — через
Claude CLI или облачный сервис по ключу. Способ выбирается в настройках.

## Что нужно

- Windows 10 или 11;
- 8 ГБ памяти (16 — комфортно), около 6 ГБ на диске вместе с моделями;
- микрофон; для записи собеседников — звук, который выводит компьютер;
- для документов — Claude CLI по подписке Claude либо ключ облачного сервиса.

Разметка говорящих работает без регистрации и токенов: модель лежит в
репозитории. Есть и релиз с разметкой через pyannote — ему нужен бесплатный
токен Hugging Face; какой релиз собран, решает `release.json`.

Интернет нужен один раз — скачать модели. Как выбрать модели распознавания
и подключить документы — «[Как устроен Hagen](./Как%20устроен%20Hagen.md)»,
раздел 4.

## Установка и запуск

**Проще всего — готовый архив** со страницы
[Releases](https://github.com/prankhitevil/Hagen/releases): файл
`Hagen-<версия>-windows.zip`, около 1,6 ГБ. В нём всё сразу: Python, библиотеки,
модель распознавания и модель разметки говорящих. Распакуйте в постоянное место
(например, `C:\Apps\Hagen`, не на рабочий стол и не в облачную папку),
запустите `Ustanovka.cmd` — он ничего не скачивает, только настраивает пути и
создаёт ярлык, — а дальше `Hagen.cmd` или ярлык «Hagen».

**Из исходников** (этот репозиторий, около 40 МБ): `Ustanovka.cmd` один раз
скачает Python (15 МБ), библиотеки (около 260 МБ), ffmpeg (115 МБ) и модель
распознавания (около 890 МБ) — всего около 1,3 ГБ.

Быстрая модель распознавания, английская модель и браузер для входа в
SharePoint в обоих случаях качаются по кнопке, когда понадобятся.

```
Ustanovka.cmd      — установить или настроить папку программы
Hagen.cmd       — запустить
```

Портативная папка: программа ничего не пишет в систему и переносится копированием.
Записи лежат в `data\`, настройки — в `settings.json`, журнал — в `logs\`.

## Проверки

```
.venv\Scripts\python.exe tests\run_all.py --fast      быстрые: без моделей, звука, браузера и сети
.venv\Scripts\python.exe tests\run_all.py             всё, кроме черновых сценариев
.venv\Scripts\python.exe tests\run_all.py --only t71,t85
.venv\Scripts\python.exe tests\run_all.py --list      что есть и кому что нужно
```

Проверки сценарные: каждая поднимает нужный кусок программы и проверяет
поведение, а не внутренности. Те, которым нужны модели, звуковые устройства,
окна Windows или сеть, в `--fast` пропускаются с указанием причины.

## Как это устроено

Служба на FastAPI слушает `127.0.0.1:8787`, окно — обычная страница в WebView2.
Распознавание — GigaAM v3 (точная модель через onnx-asr или torch, по желанию —
быстрая на onnxruntime для звонков и голосового ввода), разметка говорящих —
модель pyannote community-1 в ONNX с группировкой VBx на numpy (или сам
pyannote — в релизе с ним), поиск речи — silero-vad.
Подробнее — «[Как устроен Hagen](./Как%20устроен%20Hagen.md)».

## Лицензия и авторство

Программа распространяется на условиях свободной лицензии GNU General Public
License v3.0. Исходный текст — в файле [LICENSE](./LICENSE).

- **Правообладатель и архитектор проекта:** @prankhitevil.
- **Поддержка:** проект личный, отвечаю по возможности. Pull request'ы не
  принимаются, пожелания и ошибки — в Issues, см. [CONTRIBUTING.md](./CONTRIBUTING.md).
- **Технический ассистент:** Claude (Anthropic).
- **Как это делалось:** программа задумана, проверена и отлажена человеком.
  Написание кода, перестройка и рутинные операции выполнены ИИ-ассистентом под
  полным замыслом и контролем автора. Подробнее — «[Как устроен
  Hagen](./Как%20устроен%20Hagen.md)», раздел 6.

---

**Hagen** — local meeting recorder and transcriber for Windows. Records the
microphone and call audio, also processes existing recordings and links,
transcribes speech with GigaAM, assigns speakers with the pyannote
community-1 model running on ONNX Runtime (no account or token needed), turns
the transcript into documents and stores them in an Obsidian
vault. Speech recognition runs entirely on the local machine; audio never leaves
the computer. Interface and documentation are in Russian.
