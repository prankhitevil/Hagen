# -*- coding: utf-8 -*-
"""Розетки для Windows — то, что работает у программы сейчас.

Модули здесь переехали из `hagen/` как есть: те же строки по новому адресу.
Правки делались только там, где без них вызов через розетку не собрать, — их
видно в истории отдельными коммитами.

Подключать модули отсюда напрямую не надо: за этим есть `hagen.platform`,
и он же не даёт `pycaw`, `comtypes` и `win32com` загрузиться раньше времени.
"""
