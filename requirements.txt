# Зависимости «Hagen» — что программе нужно по смыслу.
#
# Установщик ставит НЕ по этому файлу, а по описи выпуска lock.json: там всё
# окружение со всеми зависимостями зависимостей, точными версиями и суммами.
# Поменяли что-то здесь — пересоберите опись: .venv\Scripts\python.exe
# tools\make_lock.py (она берёт версии из .venv, на котором всё проверено).
#
# torch программе НЕ нужен (решение 23.09): поиск речи, обе русские модели,
# английская и разметка голосов считаются на onnxruntime. torch, gigaam и
# silero-vad остаются в мастерской как инструменты разработчика — ими делается
# разовый экспорт новых моделей в ONNX (tools\export_asr_onnx.py); ставятся
# руками, в сборки и в опись не попадают. Релиз с разметкой через pyannote —
# отдельный набор requirements-pyannote.txt, там torch есть.

# --- распознавание речи -------------------------------------------------
onnxruntime==1.23.2
# Модели GigaAM v3 (быстрая и точная, русская речь) и NVIDIA Parakeet TDT
# (английская) — готовый ONNX с Hugging Face, считает их пакет onnx-asr.
# Пакет чистый Python, новых зависимостей не тянет (numpy, onnxruntime и
# huggingface_hub стоят и так). Веса лежат в models\onnx-asr\.
# ВНИМАНИЕ: pip создаёт лаунчер .venv\Scripts\onnx-asr.exe — его надо УДАЛИТЬ
# сразу после установки, библиотеке он не нужен, а лишний неподписанный .exe
# в проекте держать нельзя (см. правило про антивирус в README).
onnx-asr==0.12.0
# Модели распознавания качаются по кнопке с Hugging Face.
huggingface_hub==1.31.0
# Поиск речи (Silero VAD) — своя сессия onnxruntime, модель в models\vad.

# --- разделение говорящих -----------------------------------------------
# Разметка говорящих по умолчанию — ONNX + VBx: модель лежит в models\diar и
# идёт на onnxruntime, отдельных пакетов ей не нужно. Релиз с pyannote ставит
# вдобавок requirements-pyannote.txt (движок выбирает release.json).

# --- звук ---------------------------------------------------------------
PyAudioWPatch==0.2.12.8
soundfile==0.14.0
soxr==1.1.0
pycaw==20251023
comtypes==1.4.16

# --- сеть ----------------------------------------------------------------
# Проверка сертификатов по хранилищу Windows, а не по своему списку certifi.
# Без этого антивирус с проверкой HTTPS (Kaspersky) подменяет сертификат, Python
# его отвергает, и на каждую попытку выходит окно «сайт может отображаться
# неправильно». См. _use_windows_cert_store() в hagen/config.py.
truststore==0.10.4

# --- служба и окно приложения -------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.52.4
websockets==17.1
python-multipart==0.0.32
# QR-код со ссылкой на страницу для телефона (hagen/lan.py). Чистый Python без
# зависимостей. Лаунчер .venv\Scripts\segno.exe после установки удалить — он
# библиотеке не нужен.
segno==1.6.6
pywebview==6.2.1
httpx==0.28.1

# --- Windows ------------------------------------------------------------
pywin32==312
psutil==7.2.2
# Уведомления Windows с кнопками (вопросы о звонке). Тянет пять пакетов winrt,
# программ .exe среди них нет; лаунчер register_hkey_aumid.exe после установки
# удалить — программа регистрируется в реестре сама (tray.register_aumid).
windows-toasts==1.3.1
# Снимки экрана и заставка.
pillow==12.3.0

# --- видео по ссылке ----------------------------------------------------
# Здесь версия НЕ закреплена намеренно: площадки регулярно меняют выдачу, и
# рабочим остаётся только свежий yt-dlp. Установщик ставит версию из описи, а
# кнопка «Обновить yt-dlp» — свежую; обновление программы её не откатывает.
# Используется как БИБЛИОТЕКА, не как отдельная программа: лишний .exe в папке
# заставил бы антивирус спрашивать разрешение ещё и про него.
yt-dlp

# --- вычисления ---------------------------------------------------------
numpy==2.5.3
scipy==1.18.1
